title = "mcp-hugo-server-go gitleaks config"

[allowlist]
description = "Legacy agent-ready wrapper delegates to safer smoke scripts and contains no secrets."
paths = [
  '''scripts/verify-agent-ready\.sh''',
]
